# ============================================================
# Python
# ============================================================
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
.eggs/
build/
dist/
develop-eggs/
downloads/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.manifest
*.spec
# 但 packaging/atprobe.spec 是源文件（PyInstaller 构建配置），需提交
!packaging/atprobe.spec
pip-log.txt
pip-delete-this-directory.txt

# 虚拟环境
.venv/
venv/
env/
ENV/
.python-version

# 包管理器锁文件二选一：保留 uv.lock / poetry.lock，忽略 pip freeze 产物
pip freeze.txt
requirements*.txt.local

# ============================================================
# 测试 / 类型检查 / Lint 缓存
# ============================================================
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover
*.py,cover
.tox/
.nox/
cache/

# ============================================================
# 项目运行时产物（报告 / 日志 / 会话目录）
# ============================================================
# HTML 报告与用例执行会话目录（程序生成，勿提交）
reports/
logs/
logs/**
*.session.json

# 运行时配置（含本地真实串口/FTP 等环境信息，按需在根目录覆盖）
atprobe.yaml
env.yaml
# 但 examples/ 下的同名样例文件保留
!examples/**
!examples/**/atprobe.yaml
!examples/**/env.yaml

# 测试用例：仅 3GPP 通用指令用例（examples/testcases/3gpp/）纳入版本控制；
# 平台专属用例（N58/ 等按平台分组的目录）与回归用例为公司核心资产，仅保留在本地
examples/testcases/**
!examples/testcases/3gpp/
!examples/testcases/3gpp/**

# 临时/调试产物
_*.bat
_*.txt
_smoke*.py
tmp/
tmp_*
*.tmp
*.bak
*.swp
*~

# PyInstaller 构建产物（rendered spec / 临时 build 目录）
packaging/atprobe.rendered.spec
packaging/__pycache__/

# Windows 重定向产生的误生成文件
nul

# ============================================================
# IDE / 编辑器
# ============================================================
.idea/
.vscode/
*.code-workspace
.project
.pydevproject
.settings/
*.sublime-project
*.sublime-workspace

# ============================================================
# 操作系统
# ============================================================
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk
# macOS
.DS_Store
.AppleDouble
.LSOverride
# Linux
*~
.directory
.Trash-*

# ============================================================
# 项目文档
# ============================================================
# docs/ 为本地工作文档（AT 参考手册、需求/设计/计划草稿等），不纳入版本控制
docs/

# ============================================================
# 工具生成 / 构建产物
# ============================================================
# .zcode/ 为本地工具目录；packaging/build.log 为构建日志，均不纳入版本控制
.zcode/
packaging/build.log

# 本地自用 skills（pdf / skill-creator），与本项目无关，不入库
.agents/skills/pdf/
.agents/skills/skill-creator/

# skill 本地敏感参考：公司测试服务器集群真实地址（入库版只含占位值）
.agents/skills/atprobe-case-author/references/server-cluster.local.md

# 厂商 AT 指令手册 PDF 与真实设备 COM5 配置（含 IMEI/固件串），本地文件不入库
/neoway_n58_at命令手册_v2.0_20241203.pdf
examples/atprobe-com5.yaml
