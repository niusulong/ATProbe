name: PDP激活
description: 激活 PDP 上下文（使用环境配置中的 APN）
tags: [network, p1]
port: COM3

steps:
  - command: 'AT+CGDCONT=1,"IP","{{apn}}"'
    assert: { contains: "OK" }
  - command: AT+CGACT=1,1
    assert: { contains: "OK" }
    on_failure: continue

teardown:
  - command: AT+CGACT=0,1
